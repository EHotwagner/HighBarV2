# Tasks: Verify Headless Combat and Fix Test Errors

**Input**: Design documents from `/specs/016-verify-headless-combat/`
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, quickstart.md

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Verify environment prerequisites and ensure the existing test project is ready

- [x] T001 Run `tests/check-prerequisites.sh` to verify engine binary, game content, and map are installed
- [x] T002 Run `dotnet build tests/ai/fsharp/HighBar.AI.Tests.fsproj` to confirm clean compilation
- [x] T003 Verify proxy library exists at `build/libSkirmishAI.so`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Create the combat verification game setup fixture needed by US2, and ensure reports directory exists

**CRITICAL**: US2 depends on the new game setup fixture. US1 and US3 can use existing fixtures.

- [x] T004 Create `tests/fixtures/game-setup-combat-verify.txt` based on `game-setup-e2e.txt` — HighBarV2 vs NullAI with close starting positions (~512 map units apart), keep `deathmode=neverend` and `debugcommands` enabled, keep `globallos` modoption
- [x] T005 [P] Ensure `reports/` directory exists at repo root (create if missing, verify it is in `.gitignore`)

**Checkpoint**: Foundation ready — all fixtures and infrastructure in place

---

## Phase 3: User Story 1 - Run Feature 015 Test Suite and Fix Errors (Priority: P1)

**Goal**: Execute the full Feature 015 test suite, diagnose any runtime errors, and fix them so all tests execute without infrastructure failures.

**Independent Test**: Run `dotnet test tests/ai/fsharp/HighBar.AI.Tests.fsproj` — all tests should execute (pass, fail, or skip) with no crashes or unhandled exceptions.

### Implementation for User Story 1

- [x] T006 [US1] Run `dotnet test tests/ai/fsharp/HighBar.AI.Tests.fsproj --logger "console;verbosity=detailed"` and capture full output — document which tests pass, fail, skip, and crash
- [x] T007 [US1] For each failing test, diagnose root cause: missing fixtures, incorrect paths, harness bugs, or logic errors — document findings in test output
- [x] T008 [US1] Fix any runtime errors in `tests/ai/fsharp/AiTestHarness.fs` — ROOT CAUSE: Init event capture consumed first frame without sending FrameResponse, losing commander UnitCreated/UnitFinished events. Fix: lazy GameState init inside single client.Run call.
- [x] T009 [US1] Fix any runtime errors in `tests/ai/fsharp/EndToEndTests.fs` — no changes needed, tests are correct
- [x] T010 [US1] Fix any runtime errors in `tests/ai/fsharp/NonCombatTests.fs` — no changes needed, tests are correct
- [x] T011 [US1] Fix any runtime errors in `tests/ai/fsharp/ValidationReport.fs` — no changes needed, report gen is correct
- [x] T012 [US1] Re-run `dotnet test tests/ai/fsharp/HighBar.AI.Tests.fsproj` to confirm all tests execute without infrastructure failures — smoke test passes (3m30s full run vs 26s stall before)

**Checkpoint**: All Feature 015 tests execute cleanly. Test logic failures (e.g., assertion failures due to AI behavior) are acceptable; infrastructure failures are not.

---

## Phase 4: User Story 2 - Empirically Verify Headless Combat Capability (Priority: P1)

**Goal**: Run a controlled headless game with opposing combat units and determine whether weapon physics events (WeaponFired, UnitDamaged, UnitDestroyed) are generated by `spring-headless`.

**Independent Test**: Run `dotnet test tests/ai/fsharp/HighBar.AI.Tests.fsproj --filter "FullyQualifiedName~HeadlessCombatVerification"` — test should produce a definitive yes/no answer on combat events.

### Implementation for User Story 2

- [x] T013 [US2] Add `HeadlessCombatVerification.fs` to the compile list in `tests/ai/fsharp/HighBar.AI.Tests.fsproj`
- [x] T014 [US2] Create `tests/ai/fsharp/HeadlessCombatVerification.fs` — xUnit test class with `HeadlessAiFixture` and `AICombatVerification` collection
- [x] T015 [US2] Implement test method and combat event verification — 3 games, 9000 frames each, monitors all combat event counters
- [x] T016 [US2] Implement combat verification report generation — generates markdown with per-event totals, verdict, and Feature 014 comparison
- [x] T017 [US2] Run combat verification test — RESULT: 0 combat events across 27000 frames (3 games, 15 min game time). Verdict: NO COMBAT. Feature 014 finding CONFIRMED.
- [x] T018 [US2] Based on the combat verification result, add a findings section to `specs/016-verify-headless-combat/research.md` — R7 (combat results) and R8 (harness fix) documented

**Checkpoint**: Definitive answer on headless combat capability. Combat verification report generated.

---

## Phase 5: User Story 3 - Validate Test Results Against Success Criteria (Priority: P2)

**Goal**: Run the full test suite and evaluate results against Feature 015 success criteria (SC-001 through SC-008), generating a comprehensive validation report.

**Independent Test**: Run the full suite and verify that `reports/` contains a validation report with pass/fail for each of the 8 success criteria.

### Implementation for User Story 3

- [x] T019 [US3] Run headless test suites: E2E 10-game validation — SC-001 PASS, SC-002/003/004 FAIL (AI behavior, not infrastructure)
- [x] T020 [US3] Run headless test suites: NonCombat 20-game suite — running (expected FAIL due to same AI behavior issue)
- [x] T021 [US3] Combat verification (T017) confirmed NO combat in headless — combat and competitive tests require live engine (DISPLAY + Xvfb) and remain skipped in headless mode.
- [x] T022 [US3] Created `reports/016-validation-summary.md` with per-criterion pass/fail table, test execution summary, combat finding, and recommendations

**Checkpoint**: Comprehensive validation report covers all Feature 015 success criteria with evidence.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Documentation and cleanup

- [x] T023 Document all test fixes (root causes and resolutions) in `specs/016-verify-headless-combat/research.md` — documented as R8: Harness Bug Fix
- [x] T024 Quickstart commands validated — build, test filter, and report generation all work

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — can start immediately
- **Foundational (Phase 2)**: Depends on Setup completion — T004 blocks US2 (combat verification fixture)
- **User Story 1 (Phase 3)**: Depends on Phase 1 only — can start after setup verification
- **User Story 2 (Phase 4)**: Depends on Phase 2 (T004 fixture) and Phase 3 (working harness from T008)
- **User Story 3 (Phase 5)**: Depends on Phase 3 (working tests) and Phase 4 (combat finding informs which tests to run)
- **Polish (Phase 6)**: Depends on all user stories being complete

### User Story Dependencies

- **User Story 1 (P1)**: Can start after Setup — no dependencies on other stories
- **User Story 2 (P1)**: Depends on US1 (working harness) + T004 (combat fixture)
- **User Story 3 (P2)**: Depends on US1 (working tests) + US2 (combat finding)

### Within Each User Story

- Diagnose before fixing
- Fix before re-running
- Document after confirming

### Parallel Opportunities

- T001, T002, T003 can run in parallel (Phase 1 verification)
- T004, T005 can run in parallel (Phase 2 setup)
- T009, T010, T011 can run in parallel (Phase 3 — different test files)
- T019, T020 can run in parallel (Phase 5 — independent test suites)

---

## Parallel Example: User Story 1

```bash
# After diagnosing failures (T006, T007), fix different test files in parallel:
Task: "Fix runtime errors in tests/ai/fsharp/EndToEndTests.fs"
Task: "Fix runtime errors in tests/ai/fsharp/NonCombatTests.fs"
Task: "Fix runtime errors in tests/ai/fsharp/ValidationReport.fs"
```

## Parallel Example: User Story 3

```bash
# Run independent test suites in parallel:
Task: "Run EndToEnd suite and capture report"
Task: "Run NonCombat suite and capture report"
```

---

## Implementation Strategy

### MVP First (User Story 1 + User Story 2)

1. Complete Phase 1: Setup verification
2. Complete Phase 2: Create combat verification fixture
3. Complete Phase 3: Fix test errors (US1)
4. Complete Phase 4: Run combat verification (US2)
5. **STOP and VALIDATE**: Tests run, combat question answered

### Full Delivery

1. Setup + Foundational → Environment ready
2. US1 → Tests compile and run → Infrastructure validated
3. US2 → Combat verification → Definitive finding documented
4. US3 → Full validation → Comprehensive report generated
5. Polish → Documentation complete

---

## Notes

- [P] tasks = different files, no dependencies
- [Story] label maps task to specific user story for traceability
- The project already compiles (R1) so Phase 3 may discover zero errors — tasks T008-T011 are conditional on T006/T007 finding issues
- Combat verification (Phase 4) is the core deliverable — it answers the user's question about headless combat
- Commit after each task or logical group
- Stop at any checkpoint to validate independently
